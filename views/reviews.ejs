<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Reviews</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>Movie Review App</h1>
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/reviews" class="active">All Reviews</a></li>
        </ul>
      </nav>
    </header>
    
    <main>
      <section class="reviews-section">
        <h2>All Movie Reviews</h2>
        
        <% if (reviews.length === 0) { %>
          <p class="no-reviews">No reviews have been submitted yet.</p>
        <% } else { %>
          <div class="reviews-list">
            <% reviews.forEach(review => { %>
              <div class="review-card">
                <div class="review-header">
                  <h3><%= review.movieTitle %></h3>
                  <div class="rating">
                    <% for (let i = 0; i < 5; i++) { %>
                      <span class="star <%= i < review.rating ? 'filled' : '' %>">★</span>
                    <% } %>
                  </div>
                </div>
                <p class="review-meta">By <%= review.username %> <% if (review.date) { %> on <%= review.date %> <% } %></p>
                <p class="review-content"><%= review.comment %></p>
                <a href="/movie/<%= review.movieId %>" class="btn btn-small">View Movie</a>
              </div>
            <% }); %>
          </div>
        <% } %>
      </section>
    </main>
    
    <footer>
      <p>&copy; 2025 Movie Review App. All rights reserved.</p>
    </footer>
  </div>
  
  <script src="/js/script.js"></script>
</body>
</html>